<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <script src="https://cdn.socket.io/4.5.4/socket.io.min.js"></script>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="icon" href="/assets/favicon.png" type="image/png" />
    <style>
      /* Layout Flexbox untuk Sticky Footer */
      body {
        display: flex;
        flex-direction: column;
        min-height: 100vh;
        margin: 0;
      }

      /* Konten utama agar fleksibel */
      main {
        flex: 1;
      }

      /* Border Header & Footer */
      .border-line {
        width: 100%;
        height: 15px;
        background: rgba(4, 115, 234, 1);
      }

      /* Border lebih kecil di kiri */
      .border-inline {
        width: 75%; /* Sesuaikan ukuran yang diinginkan */
        height: 15px;
        background: rgba(56, 210, 0, 1);
      }

      .logo {
        margin-top: 10px;
      }
    </style>
  </head>
  <body>
    <header>
      <div class="border-line">
        <div class="border-inline"></div>
      </div>
    </header>

    <div class="timer d-flex justify-content-center mt-5 h2"></div>

    <!-- logo  -->
    <div class="d-flex justify-content-center logo">
      <img
        src="/assets/logo.png"
        class="img-fluid"
        style="width: 400px; height: 205px"
        alt="Responsive image"
        id="logo"
      />
    </div>
  </body>
</html>
